
## Installation and running:

```shell
git clone repo
npm install in each of collector/embed/server/frontend
update server/env.development file with keys
run chroma db like
docker run --rm -it --name lochromal -p 8000:8000 -d chromadb/chroma
ensure env update in env.development file
run all the services
go to localhost:3005

```

